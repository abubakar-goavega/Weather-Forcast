// jest.config.js
module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'node',
    // Optionally specify the test match pattern if needed
    // testMatch: ['**/tests/**/*.test.ts'],
  };
  